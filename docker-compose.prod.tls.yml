services:
  nginx:
    volumes:
      - ./nginx.prod.tls.conf:/etc/nginx/conf.d/default.conf:ro
      - ./certs:/etc/nginx/certs:ro
      - ./certbot/www:/var/www/certbot:ro
      - uploads_prod:/var/www/app/public/uploads:ro
    ports:
      - "80:80"
      - "443:443"