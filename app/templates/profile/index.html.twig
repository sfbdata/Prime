{% extends 'base.html.twig' %}

{% block title %}Perfil{% endblock %}

{% block body %}
    <h1>Perfil do Usuário</h1>
    <p><strong>Nome:</strong> {{ user.fullName }}</p>
    <p><strong>Email:</strong> {{ user.email }}</p>
    <p><strong>Criado em:</strong> {{ user.createdAt|date('d/m/Y H:i') }}</p>
    <p><strong>Perfis:</strong> {{ user.roles|join(', ') }}</p>

    <form action="{{ path('app_logout') }}" method="post" class="mt-3">
        <button type="submit" class="btn btn-danger">Sair da conta</button>
    </form>
{% endblock %}
