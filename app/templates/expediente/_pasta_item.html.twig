{% set grupoId = 'pasta-' ~ marcador.id %}
{% set estiloLink = marcador.cor ? 'background:' ~ marcador.cor ~ ';border-left:3px solid rgba(0,0,0,0.12);' : '' %}
<li class="expediente-menu-item" id="item-pasta-{{ marcador.id }}">
    <a href="#" class="expediente-menu-link" data-grupo="{{ grupoId }}" data-cor="{{ marcador.cor }}"
       data-marcador-id="{{ marcador.id }}"
       {% if estiloLink %}style="{{ estiloLink }}"{% endif %}>
        <i class="bi bi-chevron-right expediente-chevron"></i>
        <i class="bi bi-tag expediente-icon"></i>
        <span>{{ marcador.nome }}</span>
        <button type="button"
                class="expediente-btn-add-sub ms-auto"
                data-pai-id="{{ marcador.id }}"
                data-pai-nome="{{ marcador.nome }}"
                title="Novo sub-marcador">
            <i class="bi bi-plus-lg"></i>
        </button>
        <button type="button"
                class="expediente-btn-acao js-editar-marcador"
                data-id="{{ marcador.id }}"
                data-nome="{{ marcador.nome }}"
                data-token="{{ csrf_token('editar_marcador_' ~ marcador.id) }}"
                title="Editar marcador">
            <i class="bi bi-pencil"></i>
        </button>
        <button type="button"
                class="expediente-btn-acao js-excluir-marcador"
                data-id="{{ marcador.id }}"
                data-nome="{{ marcador.nome }}"
                data-token="{{ csrf_token('excluir_marcador_' ~ marcador.id) }}"
                title="Excluir marcador">
            <i class="bi bi-trash3"></i>
        </button>
    </a>
    <ul class="expediente-submenu" id="grupo-{{ grupoId }}">
        {% for sub in marcador.filhos %}
            {% set estiloSub = sub.cor ? 'background:' ~ sub.cor ~ ';border-left:3px solid rgba(0,0,0,0.12);' : '' %}
            <li id="item-pasta-{{ sub.id }}">
                <a href="#" class="expediente-submenu-link" data-cor="{{ sub.cor }}" data-marcador-id="{{ sub.id }}"
                   {% if estiloSub %}style="{{ estiloSub }}"{% endif %}>
                    <i class="bi bi-tag expediente-sub-icon"></i>
                    <span>{{ sub.nome }}</span>
                    <button type="button"
                            class="expediente-btn-acao expediente-btn-acao-sub js-editar-marcador ms-auto"
                            data-id="{{ sub.id }}"
                            data-nome="{{ sub.nome }}"
                            data-token="{{ csrf_token('editar_marcador_' ~ sub.id) }}"
                            title="Editar sub-marcador">
                        <i class="bi bi-pencil"></i>
                    </button>
                    <button type="button"
                            class="expediente-btn-acao expediente-btn-acao-sub js-excluir-marcador"
                            data-id="{{ sub.id }}"
                            data-nome="{{ sub.nome }}"
                            data-token="{{ csrf_token('excluir_marcador_' ~ sub.id) }}"
                            title="Excluir sub-marcador">
                        <i class="bi bi-trash3"></i>
                    </button>
                </a>
            </li>
        {% endfor %}
        <li>
            <button type="button"
                    class="expediente-submenu-link w-100 border-0 bg-transparent text-start js-abrir-modal-marcador"
                    data-pai-id="{{ marcador.id }}"
                    data-pai-nome="{{ marcador.nome }}">
                <i class="bi bi-plus expediente-sub-icon"></i>
                Novo sub-marcador
            </button>
        </li>
    </ul>
</li>
