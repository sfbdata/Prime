{% extends 'base.html.twig' %}

{% block title %}Acesso Restrito — {{ moduloLabel }}{% endblock %}

{% block body %}
<section class="content-header">
    <div class="container-fluid d-flex justify-content-between align-items-center">
        <h1 class="m-0"><i class="bi bi-lock me-2"></i>Acesso Restrito</h1>
        <a href="{{ path('homepage') }}" class="btn btn-secondary">
            <i class="bi bi-arrow-left me-1"></i> Voltar ao Início
        </a>
    </div>
</section>

<section class="content">
    <div class="container-fluid">
        <div class="card">
            <div class="card-body text-center py-5">
                <i class="bi bi-shield-lock text-secondary" style="font-size: 4rem;"></i>
                <h4 class="mt-3">Você não tem permissão para acessar este item</h4>
                <p class="text-muted mb-4">
                    O módulo <strong>{{ moduloLabel }}</strong> não está disponível para o seu perfil.<br>
                    Entre em contato com o administrador para solicitar o acesso.
                </p>
                <a href="{{ path('homepage') }}" class="btn btn-primary">
                    <i class="bi bi-house me-1"></i> Ir para o Início
                </a>
            </div>
        </div>
    </div>
</section>
{% endblock %}
