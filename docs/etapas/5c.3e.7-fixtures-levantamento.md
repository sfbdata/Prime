# Levantamento de fixtures legadas — Fase 5c.3e.7 (sprint preparatória)

## 0. Metadados

- **Commit HEAD:** 99e6b88
- **Data:** 2026-05-14
- **Escopo:** `app/src/DataFixtures/`, `app/tests/`, migrations de seed/E2E

## 1. Sumário

| Categoria | Total |
|---|---|
| DataFixtures com padrão legado | 1 arquivo, 1 linha |
| Testes funcionais: `User::setTenant()` inline | 8 arquivos, 9 linhas |
| Testes unit: `User::setTenant()` inline | 5 arquivos, 6 linhas |
| Testes com helper `logarComTenant()` duplicado | 2 variações incompatíveis |
| Testes com session inline (sem helper) | 4 arquivos |
| Total testes com `session->set('current_tenant_id')` | 6 arquivos |
| Migrations de seed com `user.tenant_id` direto | 1 linha (Version20260508170000) |

## 2. DataFixtures

### app/src/DataFixtures/AppFixtures.php

- **L132:** `$user->setTenant($tenant);` — método legado em User
- **Diagnóstico:** Cria User e seta `tenant_id` diretamente via setter. Não cria entidade
  UserTenant correspondente. O bootstrap em L146 cria perfil de admin via
  `tenantBootstrap->bootstrap()`, mas os demais usuários (advogado1, etc.) ficam sem
  vínculo UserTenant após Etapa 4.
- **Impacto:** Fixture principal do projeto — afeta todo ambiente de dev.

### app/src/DataFixtures/PermissionFixture.php

- **Diagnóstico:** Zero matches. Não usa padrões legados de User.

## 3. Testes com padrão legado

### 3.1 Testes funcionais — User::setTenant() (8 arquivos)

Todos os casos abaixo usam `$user->setTenant($tenant)` para vincular usuário a tenant
durante setup do teste. Nenhum cria UserTenant correspondente.

| Arquivo | Linhas | Observação |
|---|---|---|
| `tests/Controller/Tenant/DeleteSedeCrossTenantTest.php` | 44 | Atacante cross-tenant — caso citado no doc mestre |
| `tests/Pasta/Functional/AlterarPrioridadeControllerTest.php` | 36 | Setup básico de usuário |
| `tests/Pasta/Functional/DemandasControllerTest.php` | 37 | Setup básico de usuário |
| `tests/Pasta/Functional/PastaFinanceiroControllerTest.php` | 39 | Setup básico de usuário |
| `tests/Pasta/Functional/PastaSecaoControllerTest.php` | 38 | Setup básico; L66 também seta PastaSecao |
| `tests/Pasta/Functional/PeticionarControllerTest.php` | 51 | Setup básico de usuário |
| `tests/Profile/Functional/SalvarFotoPerfilControllerTest.php` | 37 | Setup básico de usuário |
| `tests/Tenant/Functional/DemitirFuncionarioControllerTest.php` | 43, 63 | 2 usuários (admin + funcionário) |

**Classificação:** Fixture inline (helper que cria User para contexto do teste).
Nenhum está validando o setter como feature — todos são setup de dependência.

### 3.2 Testes unit — User::setTenant() (5 arquivos)

Padrão esperado em testes unit (entidade isolada), mas registrado para completude.
Precisarão ser ajustados quando `User::setTenant()` for removido.

| Arquivo | Linhas |
|---|---|
| `tests/Tenant/Unit/DemitirFuncionarioUseCaseTest.php` | 37 |
| `tests/Tenant/Unit/TenantBootstrapServiceTest.php` | 32 |
| `tests/Pasta/Unit/EditarObservacaoFinanceiraUseCaseTest.php` | 30 |

### 3.3 setTenant em outras entidades (não são padrão legado de User)

Os seguintes matches aparecem na busca mas não são `User::setTenant()` — as entidades
têm campo `tenant_id` próprio e o padrão é válido para elas:

- `Invitation::setTenant()` — 5 matches em Auth/Unit e Auth/Functional
- `PastaSecao::setTenant()` — 6 matches em Pasta/Unit e Pasta/Functional
- `TenantRole::setTenant()` — 1 match em GerenciarConvitesControllerTest
- `ChecklistItem::setTenant()` — 1 match
- `Sede::setTenant()` — 1 match (DeleteSedeCrossTenantTest:62, entidade alvo do teste)

> **Nota de qualidade:** Validação contra HEAD a cada levantamento. Padrão de falso
> positivo detectado em 5c.3e.2 (PastaController:775) e Check 2 (ExpedienteController:39)
> mostra que pré-requisitos do doc mestre podem ficar desatualizados. Próximo levantamento
> deve revalidar se os métodos `setTenant()` nestas outras entidades foram ou não removidos
> antes de classificá-los como pendências.

## 4. Duplicação de helper de login

Não existe classe base `WebTestCase` customizada no projeto.
Todos os testes estendem `Symfony\Bundle\FrameworkBundle\Test\WebTestCase` diretamente.

**Variação 1** — `tests/Auth/Functional/GerenciarConvitesControllerTest.php:141`

```php
private function logarComTenant($client, User $user, Tenant $tenant): void
{
    $client->loginUser($user);
    $session = $client->getSession();
    $session->set('current_tenant_id', $tenant->getId());
    $session->save();
}
```

Usada em 6 chamadas dentro do mesmo arquivo. Recebe Tenant como parâmetro explícito.

**Variação 2** — `tests/Pasta/Functional/PastaSecaoControllerTest.php:75`

```php
private function logarComTenant(KernelBrowser $client, User $user): void
{
    $client->disableReboot();
    $client->loginUser($user);
    $tenant = $user->getTenant();   // depende de User::getTenant() — método legado
    // ... guard null
    $client->request('GET', '/escritorio/selecionar');
    $session = $client->getRequest()->getSession();
    $session->set('current_tenant_id', $tenant->getId());
    $session->save();
}
```

Usada em 4 chamadas dentro do mesmo arquivo. Extrai tenant do User via método legado,
faz request de warm-up e usa `disableReboot()`.

**Session inline sem helper (4 testes):**

| Arquivo | Linhas |
|---|---|
| `tests/Auth/Functional/AdminConviteControllerTest.php` | 96–99 |
| `tests/Controller/Tenant/DeleteSedeCrossTenantTest.php` | 100–102 |
| `tests/Pasta/Functional/DemandasControllerTest.php` | 46–49 |
| `tests/Tenant/Functional/DemitirFuncionarioControllerTest.php` | 194–197 |

**Total:** 6 testes com `session->set('current_tenant_id', ...)` — nenhum helper centralizado.

## 5. Migrations de seed

| Migration | Linha | Trecho | Status |
|---|---|---|---|
| `Version20260508170000.php` | 49 | `INSERT INTO user_tenant ...` | Correto |
| `Version20260508170000.php` | 60 | `UPDATE "user" SET tenant_id = 1` | **Legado** |
| `Version20260512120000.php` | 64 | `INSERT INTO user_tenant ...` | Correto |

A migration E2E (20260508170000) faz as duas coisas: cria o vínculo correto em
`user_tenant` E ainda grava em `user.tenant_id` diretamente. O comentário inline na
migration diz: "compatibilidade com código legado (ex.: sidebar)".

> **Possível falsa pendência:** O doc mestre classifica a remoção desta linha como
> bloqueador da Etapa 6, condicionada à refatoração dos templates que lêem
> `user.tenant_id`. Porém `_sidebar.html.twig` já foi refatorado na Fase 5c.1.
> Antes de tratar esta linha como bloqueador real, validar contra HEAD se ainda há
> algum template ou serviço lendo `user.tenant_id` diretamente. Pode ser falsa pendência.

## 6. Proposta de escopo da sprint de fixtures

### Sub-lote A — AppFixtures + classe base WebTestCase (base da sprint)

- Refatorar `AppFixtures.php:132` para criar UserTenant em vez de setar `User.tenant_id`
- Criar `tests/Functional/JusPrimeWebTestCase.php` com `logarComTenant()` canônico
  (recebe User; lê UserTenant ativo no banco; sem dependência de `User::getTenant()`)
- Migrar as 2 variações de helper privado para a nova base

### Sub-lote B1 — Testes funcionais Pasta (Variação 1 ou sem helper)

4 arquivos que usam `User::setTenant()` + session inline — substituir por UserTenant +
helper da base:

- `tests/Pasta/Functional/AlterarPrioridadeControllerTest.php`
- `tests/Pasta/Functional/DemandasControllerTest.php`
- `tests/Pasta/Functional/PastaFinanceiroControllerTest.php`
- `tests/Pasta/Functional/PeticionarControllerTest.php`

### Sub-lote B2 — PastaSecaoControllerTest (investigação isolada)

- `tests/Pasta/Functional/PastaSecaoControllerTest.php` — tem Variação 2 do helper
- **Investigar antes de migrar:** `disableReboot()` é workaround para bug de sessão
  pontual ou comportamento necessário globalmente? Se necessário, incluir na base;
  se pontual, isolar neste teste.

### Sub-lote C — Testes funcionais isolados

- `tests/Profile/Functional/SalvarFotoPerfilControllerTest.php`
- `tests/Tenant/Functional/DemitirFuncionarioControllerTest.php` (2 Users)
- `tests/Auth/Functional/AdminConviteControllerTest.php` (session inline)
- `tests/Controller/Tenant/DeleteSedeCrossTenantTest.php` (atacante cross-tenant — caso especial)

### Sub-lote D — Testes unit

5 arquivos: substituir `User::setTenant()` por construção via UserTenant ou factory.
Menor urgência — só bloqueiam quando `User::setTenant()` for efetivamente removido.

### Sub-lote E — Migration E2E

- Remover `UPDATE "user" SET tenant_id = 1` de `Version20260508170000.php:60`
- Somente após validar que nenhum código lê `user.tenant_id` diretamente (ver nota seção 5)
- Candidato a entrar no início da Etapa 6, não na sprint de fixtures

## 7. Pré-requisitos da sprint

1. **Decisão: classe base ou trait?**
   - Opção A: `JusPrimeWebTestCase extends WebTestCase` — todos os funcionais herdam
   - Opção B: trait `ComportamentoTenantLogin` — mixado em cada classe
   - Recomendação: classe base (mais simples, evita conflito de assinaturas entre variações)

2. **Decisão: `logarComTenant` lê UserTenant ou recebe Tenant?**
   - A Variação 1 recebe Tenant explícito
   - A Variação 2 lê via `User::getTenant()` — método legado que vai sumir na Etapa 6
   - Canônico pós-Etapa 6: buscar UserTenant ativo do usuário diretamente no banco

3. **Decisão: `disableReboot()` necessário globalmente?**
   - Apenas PastaSecaoControllerTest usa — investigar antes de B2 iniciar
   - Se for necessário globalmente, incluir na base do Sub-lote A

4. **Decisão: migration E2E entra na sprint de fixtures ou na Etapa 6?**
   - Sugestão: Etapa 6, junto com remoção da coluna `user.tenant_id`
   - Depende de validação mencionada na seção 5

5. **Buscas negativas confirmadas (HEAD 99e6b88):**
   - `setCargo()`, `setLotacao()`, `setCodigoFuncionario()`, `setDemitidoEm()`, `setLastLogin()`
   - Zero matches em DataFixtures e tests — já removidos nas etapas anteriores